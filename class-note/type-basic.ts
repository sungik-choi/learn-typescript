const str = "hello";

const str1: string = "hello";
const num: number = 10;
const arr: Array<number> = [1, 2, 3];
const items: number[] = [1, 2, 3];

// 튜플
// 배열의 특정 순서의 타입까지 정의하는 것

const address: [string, number] = ['gangnam', 100];

// 객체

const obj: object = {};

const person: { name: string, age: number } = {
  name: 'a',
  age: 1,
}

// 불리언

const isTrue: boolean = true;
